Required access: read allowed/*.

Initial finding: unnecessary reads from unrelated/*.
Change: remove the unnecessary resource ARN.

Actual before/after results and screenshot references.

The dry-run dependency issue and corrected deployment order.

Limitation: this was a controlled test using synthetic data.
